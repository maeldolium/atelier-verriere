# macOS
.DS_Store
.AppleDouble
.LSOverride

# Éditeurs / IDE
.vscode/
.idea/
*.swp

# Node (si tu rajoutes Tailwind CLI, un bundler, etc. plus tard)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json

# Build / dist (si tu génères du CSS compilé plus tard)
dist/
build/

# Variables d'environnement (si tu ajoutes un jour une clé API, etc.)
.env
.env.local

# Fichiers temporaires
*.log
*.tmp